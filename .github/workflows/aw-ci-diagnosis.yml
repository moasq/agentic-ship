# Generated by Agentic Ship GitHub Agentic Workflows compiler. DO NOT EDIT DIRECTLY.
# Source: .github/aw/ci-diagnosis.md
name: aw-ci-diagnosis

on:
  workflow_run:
    types: ["completed"]

permissions:
  contents: read
  actions: read
  issues: write

concurrency:
  group: aw-ci-diagnosis-${{ github.ref }}
  cancel-in-progress: true

jobs:
  agentic-workflow:
    name: CI Failure Diagnosis (codex)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node Environment
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run Agentic Workflow (ci-diagnosis)
        env:
          AW_ENGINE: "codex"
          AW_MAX_COST_USD: "0.15"
          AW_SAFE_OUTPUTS: "issue_comment,workflow_summary"
        run: |
          echo "Running Agentic Workflow: CI Failure Diagnosis"
          echo "Engine: codex | Budget: $${AW_MAX_COST_USD}"
