# Generated by Agentic Ship GitHub Agentic Workflows compiler. DO NOT EDIT DIRECTLY.
# Source: .github/aw/release-notes.md
name: aw-release-notes

on:
  push:
    types: ["main"]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: aw-release-notes-${{ github.ref }}
  cancel-in-progress: true

jobs:
  agentic-workflow:
    name: Release Note Drafting (claude)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node Environment
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run Agentic Workflow (release-notes)
        env:
          AW_ENGINE: "claude"
          AW_MAX_COST_USD: "0.1"
          AW_SAFE_OUTPUTS: "workflow_summary"
        run: |
          echo "Running Agentic Workflow: Release Note Drafting"
          echo "Engine: claude | Budget: $${AW_MAX_COST_USD}"
