# Generated by Agentic Ship GitHub Agentic Workflows compiler. DO NOT EDIT DIRECTLY.
# Source: .github/aw/upstream-review.md
name: aw-upstream-review

on:
  pull_request_target:
    types: ["opened"]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: aw-upstream-review-${{ github.ref }}
  cancel-in-progress: true

jobs:
  agentic-workflow:
    name: Upstream Dependency and Security Review (copilot)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node Environment
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run Agentic Workflow (upstream-review)
        env:
          AW_ENGINE: "copilot"
          AW_MAX_COST_USD: "0.2"
          AW_SAFE_OUTPUTS: "pull_request_review"
        run: |
          echo "Running Agentic Workflow: Upstream Dependency and Security Review"
          echo "Engine: copilot | Budget: $${AW_MAX_COST_USD}"
