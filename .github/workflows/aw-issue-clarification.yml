# Generated by Agentic Ship GitHub Agentic Workflows compiler. DO NOT EDIT DIRECTLY.
# Source: .github/aw/issue-clarification.md
name: aw-issue-clarification

on:
  issues:
    types: ["opened", "labeled"]

permissions:
  contents: read
  issues: write

concurrency:
  group: aw-issue-clarification-${{ github.ref }}
  cancel-in-progress: true

jobs:
  agentic-workflow:
    name: Issue Clarification (claude)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node Environment
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run Agentic Workflow (issue-clarification)
        env:
          AW_ENGINE: "claude"
          AW_MAX_COST_USD: "0.1"
          AW_SAFE_OUTPUTS: "issue_comment"
        run: |
          echo "Running Agentic Workflow: Issue Clarification"
          echo "Engine: claude | Budget: $${AW_MAX_COST_USD}"
