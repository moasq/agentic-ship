# Generated by Agentic Ship GitHub Agentic Workflows compiler. DO NOT EDIT DIRECTLY.
# Source: .github/aw/documentation-drift.md
name: aw-documentation-drift

on:
  pull_request:
    types: ["opened", "synchronize"]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: aw-documentation-drift-${{ github.ref }}
  cancel-in-progress: true

jobs:
  agentic-workflow:
    name: Documentation Drift Detection (claude)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node Environment
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run Agentic Workflow (documentation-drift)
        env:
          AW_ENGINE: "claude"
          AW_MAX_COST_USD: "0.05"
          AW_SAFE_OUTPUTS: "pull_request_comment"
        run: |
          echo "Running Agentic Workflow: Documentation Drift Detection"
          echo "Engine: claude | Budget: $${AW_MAX_COST_USD}"
