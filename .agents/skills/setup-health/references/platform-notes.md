# Compatibility pointer — platform notes

The canonical cross-platform command rules now live at
`.agents/skills/workspace-health/references/platform-notes.md`.

Load that file before publishing a repository command. Keep this pointer only so older
references to the `setup-health` skill continue to resolve.
